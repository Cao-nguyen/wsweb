mixin select-tree(items, level = 0, parent_id = "")
    each item in items
        - const prefix = Array(level + 1).join("-- ");
        option(
            value=item._id
            selected=(item._id == parent_id ? true : false)
            ) #{prefix}#{item.name}
            if item.children && item.children.length > 0
                +select-tree(item.children, level + 1, parent_id)